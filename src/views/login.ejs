<!DOCTYPE html>

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Login UI</title>
  <meta name="description" content="">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="">
  <style>
    * {
      box-sizing: border-box;
    }

    .wrapper {
      width: 40%;
      margin: auto;

    }

    /* Login form */
    #login-form {
      width: 100%;
      border-radius: 15px;
      border: 1px solid #3335FE;
      padding: 15px 20px;
    }

    #login-form label {
      display: block;
      margin: 10px auto;
    }

    #login-form label input {
      width: 100%;
      padding: 10px 12px;
      font-size: 1.2em;
      font-weight: normal;
      outline: 0px;
      border: 0px;
      color: #fff;
      background-color: #555;
    }

    #login-form label input:focus {
      background-color: #eee;
      color: #555;
    }

    #login-form button {
      padding: 10px 15px;
      font-weight: 200;
      font-size: 1.1em;
      width: 6em;
      text-align: center;
      margin: 10px 0px 10px 0px;
      color: #333;
      background-color: #4CAF50;
    }

    p#login-meta {
      margin-top: 25px;
      text-align: center;
    }

    p#login-meta a {
      border-right: 1px solid #444;
      padding: 5px 20px;
      text-decoration: none;
      color: #555;
    }

    p#login-meta a:last-of-type {
      /* margin-left: 20px; */
      border: 0;

    }
  </style>
</head>

<body>

  <div class="wrapper">
    <form name="login" id="login-form" method="POST" action="/auth/login">
      <% if(message.length > 0 ) { %>
      <small>Please, attend to these errors </small>
      <ul>
        <% message.forEach(msg => {  %>
        <li> <%= msg  %></li>

        <%}) %>
      </ul>
      <%}%>
      <label for="email">
        <input class="form-control" name="email" type="email" placeholder="Email" />
      </label>

      <label for="password">
        <input class="form-control" name="password" type="password" placeholder="Password" />
      </label>

      <button type="submit" class="btn">Login</button>
      <p id="login-meta"><a href="/auth/signup">Sign up</a> <a href="#">Forgot Password</a></p>
    </form>
  </div>

  <link rel="stylesheet" href="./rwd.css" />
</body>

</html>